%h2= @car.title
!= image_tag @image_url, :id => 'main-image'
  

#description= @car.description

%ul#thumbnails
  - for image in @car.images
    %li!= link_to image_tag(image.image.url(:thumbnail)), car_image_url(@car, image)